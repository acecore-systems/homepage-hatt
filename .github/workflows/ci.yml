name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build and Format
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check formatting
        run: npm run format:check

      - name: Validate CMS content
        run: npm run validate:content

      - name: Test CMS proxy
        run: npm run test:cms

      - name: Test shop administration access
        run: npm run test:shop

      - name: Test sitemap modification dates
        run: npm run test:sitemap

      - name: Check Pages Function bindings
        run: npm run types:functions:check

      - name: Type-check Pages Functions
        run: npm run typecheck:functions

      - name: Check Course email Worker bindings
        run: npm run types:course-email:check

      - name: Type-check Course email Worker
        run: npm run typecheck:course-email

      - name: Test Course email Worker
        run: npm run test:course-email

      - name: Check disclosure email Worker bindings
        run: npm run types:disclosure-email:check

      - name: Type-check disclosure email Worker
        run: npm run typecheck:disclosure-email

      - name: Test disclosure email Worker
        run: npm run test:disclosure-email

      - name: Build site
        run: npm run build
